# PostgreSQL connection — update credentials as needed
DATABASE_URL="postgresql://tampakan:tampakan_dev@localhost:5432/tampakan_tcc?schema=public"

# JWT secret — use a strong random value in production
JWT_SECRET="tampakan-tcc-dev-secret"

# CORS — comma-separated allowed origins
CORS_ORIGIN="http://localhost:5173,http://localhost:4173"
