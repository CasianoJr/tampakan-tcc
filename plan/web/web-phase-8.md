# Web Phase 8 — Admissions Pages

## Goal

Implement the `/admissions` page with subpages for Requirements, Enrollment Process, and Scholarships.

## Steps

1. Create `src/pages/admissions/AdmissionsPage.tsx` — hero + overview + subpage cards
2. Create `src/pages/admissions/AdmissionsRequirementsPage.tsx`
3. Create `src/pages/admissions/AdmissionsEnrollmentPage.tsx`
4. Create `src/pages/admissions/AdmissionsScholarshipsPage.tsx`
5. Update `src/router.tsx` — add `/admissions/*` routes
6. Build & verify

## Verification

```bash
npm run build
```

---

## Done

| Step | Status |
|------|--------|
| Create `AdmissionsPage` — hero + subpage cards (Requirements, Enrollment, Scholarships) | ✅ |
| Create `AdmissionsRequirementsPage` — requirements list | ✅ |
| Create `AdmissionsEnrollmentPage` — step-by-step process | ✅ |
| Create `AdmissionsScholarshipsPage` — scholarship program cards | ✅ |
| Update `router.tsx` — add `/admissions/*` routes | ✅ |
| `npm run build` — 0 errors | ✅ |
