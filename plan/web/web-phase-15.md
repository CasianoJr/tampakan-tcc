# Web Phase 15 — `/gallery`

## Goal

Build the Gallery page with category filtering and lightbox.

## Steps

1. Create `src/data/gallery.ts` — gallery photos with captions and categories
2. Create `src/pages/GalleryPage.tsx` — hero + category filter + masonry grid + lightbox modal
3. Add `/gallery` route to `src/router.tsx`
4. Build & verify

## Verification

```bash
npm run build
```

## Done

- Created `src/data/gallery.ts` — 8 gallery photos across 3 categories
- Created `src/pages/GalleryPage.tsx` — hero + category filter + masonry grid with hover captions + lightbox modal
- Added `/gallery` route to `src/router.tsx`
- Build: 0 errors
