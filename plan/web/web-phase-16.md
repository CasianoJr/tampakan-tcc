# Web Phase 16 — `/faq`

## Goal

Build the FAQ page with category filtering and accordion.

## Steps

1. Create `src/data/faq.ts` — FAQ items with categories
2. Create `src/pages/FaqPage.tsx` — hero + category filter + accordion FAQ
3. Add `/faq` route to `src/router.tsx`
4. Build & verify

## Verification

```bash
npm run build
```

## Done

- Created `src/data/faq.ts` — 12 FAQ items across 5 categories
- Created `src/pages/FaqPage.tsx` — hero + category filter + accordion with expand/collapse
- Added `/faq` route to `src/router.tsx`
- Build: 0 errors
