# Web Phase 13 — `/contact`

## Goal

Build the Contact page with form and contact information.

## Steps

1. Create `src/data/contact.ts` — contact info and department options
2. Create `src/pages/ContactPage.tsx` — hero + contact form + contact info + map placeholder
3. Add `/contact` route to `src/router.tsx`
4. Build & verify

## Verification

```bash
npm run build
```

## Done

- Created `src/data/contact.ts` — contact info and department options
- Created `src/pages/ContactPage.tsx` — hero + form with submit confirmation + contact info + map placeholder
- Added `/contact` route to `src/router.tsx`
- Build: 0 errors
