# Web Phase 14 — `/downloads`

## Goal

Build the Downloads page with category filtering.

## Steps

1. Create `src/data/downloads.ts` — downloadable files with metadata
2. Create `src/pages/DownloadsPage.tsx` — hero + category filter + download list
3. Add `/downloads` route to `src/router.tsx`
4. Build & verify

## Verification

```bash
npm run build
```

## Done

- Created `src/data/downloads.ts` — 10 downloadable forms/guides across 3 categories
- Created `src/pages/DownloadsPage.tsx` — hero + category filter + download list with file type/size badges
- Added `/downloads` route to `src/router.tsx`
- Build: 0 errors
